export type ApiData = {
  bitcoin: {
    usd: number;
    usd_24h_change: number;
  };
  dogecoin: {
    usd: number;
    usd_24h_change: number;
  };
  ethereum: {
    usd: number;
    usd_24h_change: number;
  };
};
